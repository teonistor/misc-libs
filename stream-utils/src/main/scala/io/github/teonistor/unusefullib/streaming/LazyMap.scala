package io.github.teonistor.unusefullib.streaming

/*class LazyMap[+A] extends AbstractSeq[A] with IterableOps[A, LazyMap, LazyMap[A]] {
  override def apply(i: Int): A = ???

  override def length: Int = ???

  override def iterator: Iterator[A] = ???
}

object LazyMap extends SeqFactory[LazyMap] {
  override def from[A](source: IterableOnce[A]): LazyMap[A] = ???

  override def empty[A]: LazyMap[A] = ???

  override def newBuilder[A]: mutable.Builder[A, LazyMap[A]] = ???
}*/
